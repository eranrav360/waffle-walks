{
  "name": "waffle-walks-server",
  "version": "1.0.0",
  "description": "WaffleWalks backend — Express + PostgreSQL + WAHA",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "resend": "^3.5.0",
    "node-cron": "^3.0.3",
    "pg": "^8.11.5"
  }
}
